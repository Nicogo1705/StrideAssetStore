@* Copyright (c) 2026 Nicogo1705 - MIT license *@
<Router AppAssembly="typeof(Routes).Assembly" AdditionalAssemblies="_uiAssembly">
    <Found Context="routeData">
        <RouteView RouteData="routeData" DefaultLayout="typeof(StrideAssetStore.App.Layout.MainLayout)" />
        <FocusOnNavigate RouteData="routeData" Selector="h1" />
    </Found>
    <NotFound>
        <LayoutView Layout="typeof(StrideAssetStore.App.Layout.MainLayout)">
            <a class="back" href="">← Back to catalog</a>
            <h1 style="margin:.2rem 0 1rem">Page not found</h1>
            <p class="install-hint">That page doesn't exist. It may have been moved, or the link is wrong.</p>
        </LayoutView>
    </NotFound>
</Router>

@code {
    // Routable pages (Home, AssetDetail, Publish) live in the shared StrideAssetStore.UI RCL.
    private readonly System.Reflection.Assembly[] _uiAssembly =
        [typeof(StrideAssetStore.App.ServiceCollectionExtensions).Assembly];
}
